# Ronda 5 — Implementaciones

**Fecha:** 2026-09-14 21:53

## Integración del PR #1 (`audit-improvements` → master)

Merge `git merge FETCH_HEAD --no-commit --no-ff` con resoluciones:

| Fichero | Resolución | Motivo |
|---|---|---|
| `LICENSE` | Aceptado (nuevo) | MIT, referenciado por el badge del README que apuntaba a un fichero inexistente. |
| `Cargo.toml` | Aceptado + ajuste | `default-members` excluye `stinger` (binarios embebidos con `include_bytes!`); **eliminado `agents/weaver`** de la lista y del comentario (crate borrado en ronda 2). |
| `c2_channels.rs` | Auto-merge aceptado | TLS: `danger_accept_invalid_certs` solo con `HIVE_LAB_MODE`. |
| `smoke_signals.rs` | Auto-merge aceptado | Ídem. |
| `nectar.rs` | **Extensión propia** | El mismo fix TLS en `execute_storm`, que el PR no cubría. |
| `crypto.rs` | Auto-merge aceptado | Nueva `colony_key()` derivada de `HIVE_MASTER_KEY` con fallback documentado. |
| `stigmergy.rs` | Master + port manual | `TRAIL_SEED` constante embebida eliminada; `encrypt_trail`/`decrypt_trail` usan `colony_key()`. |
| `lib.rs` | Master + port manual | Export `colony_key` añadido a la línea de `pub use crypto::`. |
| `fileless.rs` | Master + port manual | Fix de doble `close()`: el `File` dueño del fd ya lo cierra al hacer drop (evita cerrar un fd reutilizado por otro hilo). El gate `HIVE_LAB_AUTHORIZED` de la ronda 4 se conserva. |
| `honeycomb.rs` | Master + reescritura | Ver sección siguiente. |
| `comms.rs` | Master (rechazado hunk del PR) | En master `ensure_failover` ya usa `tokio::sync::Mutex`; el fix del PR apuntaba a un `std::Mutex` que ya no existe. |
| `config.rs` | Master (rechazado hunk) | Los paths (`hive.toml`, `/etc/swarm/hive.toml`, `.config`) ya estaban incorporados. |
| `phoenix.rs` | Master (rechazado hunk) | Phoenix ya es simulado desde la ronda 4; el dry-run del PR quedó obsoleto. |
| `chrononaut.rs` | **`git rm` (modify/delete)** | Eliminado en ronda 3; el merge no lo resucita. |

## honeycomb → EMULADO (ronda 5)

Reescritura completa con API pública intacta:

- `install_persistence()` → siempre `false`, telemetría etiquetada
  `HONEYCOMB (simulated)`, cero escrituras (crontab/systemd/bashrc fuera).
- `install_uefi_bootkit()` → ya no sobrescribe `bootx64.efi` ni hace chmod;
  localiza (solo lectura) la ruta que *habría* sobrescrito y devuelve
  `Ok("simulated: bootkit would overwrite <path> ...")`.
- `generate_bootkit_stub()` → `Vec::new()` (no se genera payload).
- `uefi_bootkit_feasible()` / `bootkit_installed()` → reales (solo lectura).
- `uninstall_persistence()` / `remove_uefi_bootkit()` → **remediación real**:
  solo eliminan artefactos propios (`HIVE_PERSISTENCE_MARKER`, `.hive_bak`).
  Reemplaza el `crontab -r` original (borraría TODO el crontab del usuario) por
  filtrado por marker + reescritura por stdin; además se **estrecha el filtro
  respecto al PR** (quitamos `@reboot`/`nohup` que podían borrar entradas
  ajenas). Respeta `HIVE_PERSISTENCE_DRY_RUN=1`.
- Tests: `test_bootkit_stub_emulation`, `test_install_persistence_simulated`
  (verifica que no aparece la unidad systemd), `test_uninstall_dry_run_never_touches_host`,
  `test_feasibility_check`.
