# Ronda 6 — Implementaciones

**Fecha:** 2026-09-14 22:20

## Eliminaciones (git rm)

hive_base/src: `exploits.rs`, `exfil.rs`, `nectar.rs`, `leech.rs`,
`saboteur.rs`, `kerberos.rs`, `smb.rs`, `hades_gate.rs`, `stack_spoof.rs`,
`anti_analysis.rs`, `anti_forensics.rs`, `cloud_worker.rs`, `honeycomb.rs`,
`seer.rs`, `channel_rotator.rs`, `death_dance.rs`.
agents/swarm/ (crate worm completo). `syscalls.rs` sección `windows`
(213–682: NT syscalls directas, Hell's/Halo's Gate, inyección).

## Podas

- `privesc.rs`: fuera `attempt_escalation` + `ExploitTracker` + sus tests;
  queda `scan_privilege_escalation` (real, solo lectura).
- `lateral.rs`: reescrito — solo `discover_hosts` (ping sweep).
- `phoenix.rs`: fuera `hide`/`hide_fragment`/`install_persistence`/
  `rebuild_from_genome`/`scan_for_fragments`/`PersistenceMechanism`/
  `stored_path`; header documental actualizado.
- `swarming.rs`: `initiate_swarm` (SSH+keys) → `evaluate_swarm` (decisión
  documental, nunca success); test nuevo que lo verifica.
- `config.rs`: fuera `ExploitsConfig`, `AntiAnalysisConfig`, `SwarmConfig`
  (colony), `dns_domain`/`dns_resolver`; URL por defecto `/beacon`.
- `comms.rs`: fuera `send_harvest`, `escalate_privileges`, `pivot_cloud`,
  `exploit_tracker`; imports limpiados.
- `task_poller.rs`: `exfil`/`encrypt`/`wipe`/`destroy`/`sabotage` →
  status `rejected` (antes `simulated`); tests actualizados.
- `utils.rs`: `safe_init` sin delay evasivo ni anti-análisis.

## Consumidores adaptados

- honeybee: reescrito (sin execute_encrypt/exfiltrate/destroy, sin
  privesc/cloud/leech timers, sin discover_targets; genoma en memoria;
  propuestas destructivas rechazadas con log).
- queen: fuera seer/leech/privesc/cloud timers + `seeds_phoenix`; genoma
  en memoria; se conservan hivemind/tournament/whisper/c2/shell.
- drone: `make_decision` sin seer/ataques; regeneración con genoma en
  memoria (sin hide_fragment ni escrituras en /dev/shm).
- worker: fuera `run_sabotage_scan` + `collect_seer_telemetry` + timer.
- beekeeper: fuera checks anti_analysis, display de exploits/colony;
  scenario cmd reescrito (5 fases reales).
- buzz: fuera modo aggressive + worm launch.
- c2: fuera `/collect` + `collect_handler` + `loot_dir` + `Collect*` DTOs;
  `db.rs` sin tabla `exfils`/`record_exfil`; `counts()` → `beacon_count()`.
- lib.rs: 17 mods menos; re-exports podados (solo `discover_hosts`,
  genoma phoenix, privesc types sin ExploitTracker).

## Scripts/tests

- hive.toml: sin [exploits]/[anti_analysis]/[colony]/dns; url /beacon.
- docker-compose, compose.lab, helm values, scripts: `HIVE_C2_URL=…/beacon`.
- tests/hive_colony_verify.sh: sin mock exfil data, sin /collect, sin loot,
  sin swarm bin; paso 7 verifica rechazo de tareas destructivas.
- tests/e2e_colony.sh: sin collect/base64-loot; test 6 = cola acepta tarea
  `exfil` (201) y el agente la rechaza.
- tests/c2_server.py: reescrito (solo /beacon, /health, /logs, dashboard;
  sin /collect, sin loot, sin /jndi).
- tests/monitor_detections.py: fuera reglas saboteur/chrononaut.
- scripts/scenario.sh: reescrito como Colony Lifecycle Orchestrator.
