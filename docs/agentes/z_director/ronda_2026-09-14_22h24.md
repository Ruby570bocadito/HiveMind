# Informe del Director — Ronda 2

- **Fecha:** 2026-09-14
- **Hora:** 22h24 (Europe/Madrid)
- **Agente:** z_director
- **Ronda:** 2

## Resumen ejecutivo

Ronda 2 ejecutada bajo mandato explícito del propietario: aprobó la eliminación del riesgo #2 de la ronda 1 (contenido ofensivo residual). Se eliminó por completo el agente Weaver (crate, docker, helm, scripts, embeds del dropper) y cuatro módulos ofensivos de `hive_base` (`attack`, `lolbin`, `larva`, `wax`) más `ephemeral` (código muerto dependiente de `wax`), así como los documentos `docs/EVASION.md` y `docs/MITRE_MAPPING.md`. En total ~2.400 líneas de capacidad ofensiva fuera del binario y del repo. El workspace compila limpio, los tests verificados locales pasan (13/13 config, 3/3 c2-server) y toda la documentación quedó alineada. No se añadió ni mejoró ninguna capacidad ofensiva.

## Estado del proyecto

🟡 **AMARILLO (mejorando)** — Justificación:

- ✅ Build verde (`cargo check --workspace` exit 0) tras cirugía amplia; sin warnings.
- ✅ Contenido de extorsión/ransomware (ronda 1) y now ofuscación polimórfica + ATT&CK coverage + LOLBins + larvas (ronda 2) eliminados.
- ⚠️ **Queda contenido ofensivo relevante no aprobado aún**: `propaganda.rs` (phishing con IA), además de `exploits`, `lateral`, `kerberos`, `smb`, `privesc`, `exfil`, `saboteur`, `phoenix`, `anti_forensics`, `hades_gate`, `smoke_signals`, `chrononaut`, `fileless`. Decisión del propietario pendiente módulo a módulo (propuesta en recomendaciones).
- ⚠️ Suite completa sin re-verificación local (build de tests >10 min); CI debe confirmar.
- ⚠️ Divergencia `master`/`origin/main` sin resolver (se arrastra de ronda 1).

## Revisión de cada agente (ronda 2)

- **z_implementaciones:** cirugía completa y limpia; consumidores ajustados sin romper el protocolo (Role::Weaver se conserva en el enum LdC para compatibilidad de mensajería, sin binario detrás).
- **z_pulimiento:** 9 documentos actualizados, 2 eliminados; docker/compose/helm/scripts/tests de laboratorio coherentes; de paso desapareció un YAML inválido preexistente en `docker-compose.lab.yml`.
- **z_bugs:** 3 clases de rotura potencial detectadas y corregidas antes de que llegaran a CI (stinger Windows, builds `-p weaver`, YAML inválido).

## Riesgos identificados (priorizados)

| # | Riesgo | Prioridad | Nota |
|---|--------|-----------|------|
| 1 | `propaganda.rs`: phishing/social-engineering con IA | **Alta** | Recomiendo eliminarlo en ronda 3; no estaba en la lista aprobada de esta ronda |
| 2 | Resto de módulos ofensivos (exploits, lateral, privesc, exfil, kerberos, smb, saboteur, phoenix, anti_forensics, hades_gate, chrononaut, fileless) | **Alta** | Requieren clasificación explícita del propietario: borrar / conservar como referencia forense / reencuadrar defensivo |
| 3 | Divergencia `master` vs `origin/main` | Media | Convergencia planificada pendiente |
| 4 | Suite completa sin confirmación de CI | Media | La cifra de tests pública cambia esta ronda (−1 test de cobertura ATT&CK) |
| 5 | Token de GitHub usado para push (suministrado por el propietario) | Media | Uso transitorio, no persistido en config ni archivos; **rotar tras el push** |

## Recomendaciones para la próxima ronda

- **Propietario:** decidir el destino de `propaganda.rs` y del resto de módulos ofensivos (lista arriba); confirmar rotación del token.
- **z_implementaciones:** si se aprueba, pivot defensivo de `training/` (telemetría de acciones simuladas → datasets de detección); task poller de agentes (roadmap).
- **z_pulimiento:** retirar el campo legado `weaver_mutation_interval_secs` de config si se decide romper compatibilidad de `hive.toml`; revisar `pandas` en requirements.
- **z_bugs:** rate-limiting y CORS restrictivo en C2; TLS en despliegue; documentar `--api-key` en DEPLOYMENT.md.

## Decisiones del Director en esta ronda

1. Ejecutar exactamente la lista aprobada (weaver + 4 módulos + 2 docs); NO ampliar el borrado a `propaganda.rs` ni otros módulos sin aprobación explícita.
2. Conservar `Role::Weaver` en el protocolo LdC y el campo `weaver_mutation_interval_secs` en config (compatibilidad serde/mensajería; rol sin binario).
3. Push a `master` con el token suministrado por el propietario, de forma transitoria y sin persistirlo, a petición expresa del mismo.
4. `docs/archive/` se conserva intacto (registro histórico de diseño, no código ejecutable).
