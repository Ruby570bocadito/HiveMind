# Informe de Bugs/Seguridad — Ronda 7 (2026-09-15)

## Verificación de build y tests

- `cargo check --workspace`: **OK** (única advertencia: future-incompat de
  `rlua 0.19.8`, ya trazada en ROADMAP como migración a `mlua`).
- `cargo test --workspace`: **275/275, 0 fallos** — 226 lib (hive_base) +
  4 edr_sandbox + 9 integration + 28 phase_a + 2 seq + 6 c2-server.
  (Antes: 278; los 3 tests de `reactive_llm` desaparecen con el módulo.)
- Estabilidad: suite lib ejecutada 5× consecutivas verdes; workspace completo 3×.

## Observación de flakiness (no reproducida)

- En la PRIMERA ejecución post-cambio (con recompilación total del workspace en
  paralelo), un test de lib falló una única vez; el nombre concreto no quedó en
  el log filtrado (el filtro capturó `shell_without_command_is_error ... ok`,
  que pasa siempre — el fallo real fue otro test no identificado).
- 7 ejecuciones posteriores (5× lib + 2× workspace completo, incluida una con
  log íntegro) no reprodujeron el fallo. Hipótesis: test sensible a timing o a
  puertos bajo carga extrema de compilación. **Acción**: sin cambios; si la CI
  de GitHub lo reproduce, aislar el test y dotarlo de sincronización explícita.

## Auditoría de superficie

- `rg "reactive_llm|io_uring|ringreaper|llm_mutate"` sobre todo código Rust:
  **0 coincidencias**.
- `ollama` solo en `agents/queen/src/main.rs` (LLM estratégico con fallback) y
  en los compose que lo provisionan — legítimo, se conserva.
- Formato del `.bin` embebido verificado por parseo independiente: header
  100/3/14, 100 árboles, consumo exacto de 242.556/242.556 bytes.

## Seguridad del proceso

- Sin credenciales almacenadas; el token filtrado (`ghp_21rJ…`) sigue sin ser
  reutilizado y pendiente de revocación por el propietario.
- Push de las rondas 5–7 preparado vía bundles + patches en
  `/home/z/my-project/download/hivemind-push/` (el bundle de rondas 5–6 cubre
  hasta `8a7fe8f`; el de ronda 7 se regenerará tras este commit).
