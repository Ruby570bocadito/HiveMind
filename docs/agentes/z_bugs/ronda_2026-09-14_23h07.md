# Ronda 4 — Bugs/Seguridad (2026-09-14 23h07)

## Verificación de build y tests

- `cargo check --workspace`: **OK** (10 crates; única nota la future-incompat
  de `rlua`, conocida y en roadmap).
- `cargo test -p hive_base --lib`: **270/270** (incluye 5 nuevos de
  task_poller y los adaptados de privesc/saboteur).
- `cargo test -p hive_base --test integration_test`: **9/9**; `--test
  phase_a_integration`: **28/28**.
- `cargo test -p c2-server`: **6/6** (auth + rate-limit).
- `bash -n`: hive.sh, lab_setup.sh, launch_colony.sh, scenario.sh,
  hive_colony_verify.sh — OK. `ast.parse`: monitor_detections.py,
  validate_edr.py — OK.
- `cargo clippy`: no instalado en este entorno (igual que rondas previas);
  lo cubre el CI (`clippy -D warnings`).

## Bugs encontrados y corregidos durante la ronda

- `exec_ssh` de `lateral.rs` reconstruida con su firma original de 5
  parámetros (consumida por `swarming.rs` con 5 args) y gate BRAIN conservado.
- `phoenix.rs`: `self_heal`/`reassemble_genome` con collect de referencias
  correcto; convención de error documentada (ids ausentes / sentinela
  `u32::MAX`).
- Tests de `privesc` referenciando exploits eliminados → sustituidos por
  tests del contrato simulado.
- Warning `dead_code` de `is_root` (sin llamadores tras la emulación) →
  eliminadas ambas variantes cfg.
- Import `warn` no usado en `privesc.rs` → limpiado.

## Auditoría de no-ofensividad (rg sobre el árbol final)

- `ransom|extorsi|rescate|bitcoin|monero`: solo menciones históricas
  (README/AGENTS "fue eliminado", guards de simulación en honeybee). Los 7
  ficheros de `docs/archive/` con narrativa de extorsión **eliminados** esta
  ronda (sobrevivieron al merge de ronda 3).
- Lecturas de credenciales: `/etc/shadow` solo en existencia-check y
  comentarios; nada de `/proc/*/mem`, LSASS ni SAM.
- Exfil: cero syscalls de red en `exfil.rs` (planificación pura).
- Persistencia: cero escrituras en `phoenix.rs`.
- Explotación/ofuscación/stagers: `exploits.rs` sin código de ataque;
  `obfuscate_pe.py`, `build_payload.sh`, `deploy.sh` eliminados.
- Windows: maquinaria NtCreateSection y resolución de SSNs fuera;
  `hades_gate`/`stack_spoof` degradan a `None`/vacío con telemetría.

## Riesgos residuales (honestos)

- `remote_shell::execute_command` sigue siendo ejecución real de comandos
  (función operadora del C2, con auditoría en telemetría); el gate duro
  adicional requeriría allowlist por config — propuesto para ronda 5.
- `discover_hosts` y `scan_privilege_escalation`/`scan_for_targets` hacen
  operaciones reales de red/FS de SOLO LECTURA; apropiadas en lab, etiquetar
  también su telemetría con `recon` explícito sería un plus.
- Suite completa de `hive_base --lib` verde en esta máquina (270 tests,
  1.56 s); CI canónico pendiente de validar en GitHub Actions.
