# Ronda 6 — Bugs / Seguridad

**Fecha:** 2026-09-14 22:20

## Verificación

- `cargo check --workspace`: OK (solo warnings preexistentes: rlua
  future-incompat, modelos ausentes, stinger sin binarios release).
- `cargo test -p hive_base`: **272/272** (229 lib + 4 edr_sandbox +
  9 integration + 28 phase_a + 2 seq_zero).
- `cargo test -p c2-server`: **6/6**.
- Total: **278/278** — coincide con el badge/README.
- `bash -n` OK en scenario.sh, hive_colony_verify.sh, e2e_colony.sh,
  test_c2_failover.sh; `py_compile` OK en c2_server.py y
  monitor_detections.py.
- rg final: cero referencias activas a exfil/collect/saboteur/leech/
  exploits/honeycomb/nectar en código, scripts y docker (solo restos
  históricos en docs/agentes/, que son archivo).

## Seguridad

1. **Superficie reducida a cero ofensivo**: 17 módulos + crate worm +
   endpoint /collect + loot dir + callback JNDI eliminados. El binario ya no
   puede exfiltrar, persistir, escalar, propagarse ni evadir: las funciones
   no existen.
2. **server/banner**: el C2 ya no anuncia `/collect`; default URL de agentes
   → `/beacon`.
3. **Tests herméticos**: las pruebas ya no crean datos mock de "víctima"
   (ledger.csv, .aws/credentials, id_rsa mock) en /tmp.
4. **Door-left-open check**: `fileless::spawn` sigue tras `HIVE_LAB_AUTHORIZED=1`
   (primitiva de ejecución, no destructiva) — documentado en CAPABILITIES.md.
5. Pendiente histórico: revocar el token ghp_ filtrado en chat (ronda 1).
   Push de esta ronda pendiente de credenciales nuevas; NO reutilizar el
   token filtrado.
