# Informe de Pulimiento — Ronda 7 (2026-09-15)

## README.md

- Badge de tests: 278 → **275 passing**.
- Desglose de suites (2 menciones): "229 unit" → **226 unit** (caen 3 tests de
  `reactive_llm` con el módulo); integración (43) y c2-server (6) sin cambios.
- Conteo de módulos: `config (45 modules)` → **43** (43 `pub mod` reales en
  `hive_base/src/lib.rs` tras la poda).
- Honest limitations: la limitación "converter pending / pre-trained fixture"
  se reescribe: el `.bin` embebido sigue siendo un fixture pre-entrenado, pero
  la regeneración ya es reproducible end-to-end (`generate_dataset.py` →
  `train_classifier.py` → `export_bin.py` con validación).
- Línea del árbol: `training/` ahora menciona el `.bin exporter`.

## docs/CAPABILITIES.md

- Nueva sección **"Eliminado en ronda 7 (dead-paths de evasión sin
  consumidores)"** con `reactive_llm`, `io_uring_ops` y `train_model.py`,
  explicando que eran código de evasión real en dead-path (no simulación) y
  que se eliminan bajo el mismo mandato de la ronda 6.

## docs/AGENTS.md

- Fila ML de la tabla de arquitectura: "export a runtime pendiente" → pipeline
  reproducible (dataset → train → `export_bin.py` → `.bin`).
- Fila Evasión: "10 capas (IPC fileless, syscalls, anti-debug, ...)" — heredada
  y FALSA desde la ronda 6 — corregida a: ELIMINADA (ronda 6–7); solo quedan
  opsec timing y transporte TLS.
- Nueva **Nota ronda 7** con el alcance y los tests (275 / 226 unit).

## ROADMAP.md

- Ítem **ML model converter**: `[ ]` → `[✅]` (ronda 7), describiendo
  `export_bin.py`, el wiring en `train_classifier.py` y la validación de
  paridad contra sklearn.

## Coherencia global

- Ningún doc menciona ya `reactive_llm` / `io_uring_ops` como capacidades
  vivas; solo como histórico de eliminación.
- El resto de menciones a Ollama (docker-compose, lab, README) corresponden al
  LLM estratégico de `queen`, que se conserva.
