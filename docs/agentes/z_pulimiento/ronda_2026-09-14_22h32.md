# Informe de Pulimiento — Ronda 3

- **Fecha:** 2026-09-14
- **Hora:** 22h32 (Europe/Madrid)
- **Agente:** z_pulimiento
- **Ronda:** 3

## Documentación alineada con el código

| Documento | Cambio |
|---|---|
| `docs/PLAYBOOK.md` | Diagrama de arquitectura: fuera `Chrononaut` y `Wax` (este último aún aparecía pese a eliminarse en ronda 2). Playbook 5 (Cápsulas del Tiempo): de "DESHABILITADO" a "ELIMINADO", con nota de que el módulo se borró de `hive_base` en la ronda 3 por ser persistencia real (timestomping + backdoor systemd/cron); se conserva el comando forense `getfattr` para despliegues antiguos. |
| `docs/AGENTS.md` | Nota forense de Honeybee ampliada: además de la eliminación del binario (ronda 1), los módulos `chrononaut` y `propaganda` se eliminaron de `hive_base` (ronda 3). |
| `docs/API.md` | Nueva sección **"Server flags (hardening)"** con tabla completa: `--port`, `--api-key` (documentado por primera vez; existía desde la ronda 1 sin documentar), `--rate-limit`, `--cors-origin` y `--cors-anywhere`, más el orden de capas (CORS → rate-limit → auth) y los códigos de error (`401`, `429`). |
| `README.md` | Contador de tests 366 → 367 (−2 tests de crono en phase_a, +3 del rate-limiter) con desglose actualizado. |
| `scripts/scenario.sh` | Narrativa de FASE 3 y tabla del reporte de campaña sin Chrononaut, exfiltración marcada como simulada. |

## Coherencia verificada

- `rg "propaganda|chrononaut"` sobre `hive_base/src/` → sin resultados.
- Menciones restantes legítimas: informes históricos de rondas (no se alteran, son el registro del protocolo), comentarios de intención en `phase_a_integration.rs` (documentan la eliminación), `tests/monitor_detections.py` (reglas de detección de timestomping conservadas como capacidad defensiva — detectan la técnica aunque el módulo ya no exista, útil contra despliegues antiguos u otras amenazas) y `scripts/scenario.sh` (nota de eliminación).
- Diagrama PLAYBOOK ↔ `beekeeper --status` (banner) ↔ módulos reales en `hive_base/src/lib.rs`: coherentes entre sí.
- `README` ↔ `docs/API.md` ↔ flags reales de `c2 --help`: coherentes (verificado contra `Args` en `c2/src/main.rs`).

## Limpieza menor observada (sin tocar)

- El diagrama del PLAYBOOK tenía un desalineado cosmético preexistente en las cajas ASCII (ancho de `│Worker│`); se respetó el estilo original salvo la fila de módulos eliminados.
- `docs/Diseño.md`, `docs/Actualizacion.md`, `docs/Debate.md`, `docs/Final.md`, `docs/Swarm.md` y `docs/Docs.md` (solo presentes en `origin/main`) entrarán al repo con la convergencia de esta ronda; revisión de contenido pendiente para ronda 4 si el propietario quiere auditoría de esos documentos heredados.

## Verificación

- `bash -n scripts/scenario.sh scripts/deploy.sh hive.sh` → OK.
- Enlaces internos de `docs/API.md` intactos tras insertar la nueva sección.
