# Informe de Pulimiento — Ronda 9 (2026-09-15)

## README.md

- Badge y desglose de tests: 281→286 (232→237 unit, incluye los modelos loom).
- Tabla "Engineering quality": la fila CI ahora describe lo que el workflow
  REALMENTE hace (fmt + clippy bloqueantes, job loom, ML end-to-end,
  artifacts). Varios claims del README ("fmt --check", "clippy -D warnings",
  "release artifacts") eran aspiracionales y esta ronda los hace verdad.

## ROADMAP.md

- `[x]` Concurrency test suite del arena (ronda 9) con el detalle de los 3
  modelos y el gotcha de construcción de atómicos de loom.
- Entrada "CI repair" reescrita con honestidad: la ronda 8 modernizó jobs
  pero NO tocó el bloque `on:`; el fix del trigger solo aterrizó en ronda 9.
- `[x]` Arena registry memory-model hardening (ronda 9) — protocolo CAS
  RESERVED → fill → fetch_or publish, DEAD pegajoso, role atómico.
- `[x]` Dual-use hygiene sweep (ronda 9) — PrivEscResult fuera, cabeceras de
  decisión en privesc/remote_shell.
- P2 lint tightening: nota de que `-D warnings` + `fmt --check` ya son gate
  base (queda `unwrap_used` por módulo).

## docs/AGENTS.md

- Nota ronda 8: corrección explícita añadida (⚠️ el diff no tocó `on:`).
- Nota ronda 9 completa: protocolo del arena, loom-model, CI real, higiene,
  286 (237 unit).

## docs/CAPABILITIES.md

- Nueva sección "Ronda 9 — endurecimiento del arena e higiene dual-use":
  protocolo de memoria antes/después, verificación loom, higiene dual-use y
  estado real de la CI.

## Formato

- `cargo fmt` aplicado a todo el árbol (cambios cosméticos; primera vez que
  el repo pasa `fmt --check` — el componente ni siquiera estaba instalado
  hasta esta ronda). Gate añadido a CI.
