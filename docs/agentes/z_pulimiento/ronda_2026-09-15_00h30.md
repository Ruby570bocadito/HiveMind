# Informe de Pulimiento — Ronda 8 (2026-09-15)

## README.md

- Badge y desgloses de tests: 275 → **281**; unit 226 → **232** (incluye la
  consola Lua y el roundtrip del modelo embebido).
- Fila de la tabla: se explicita que las pruebas unitarias cubren la consola
  Lua y el roundtrip ML.

## ROADMAP.md

- **mlua migration**: `[ ]` → `[x]` (ronda 8) con detalle de la migración y
  de la desaparición del future-incompat de `cargo check`.
- **Split de hive_base por features**: se mantiene abierto pero con el
  análisis de la ronda 8 (módulos load-bearing, coste/beneficio, criterios de
  reintroducción) — decisión transparente en lugar de tarea huérfana.
- **Fix or delete remaining dead paths**: `[ ]` → `[x]` (completado en la
  ronda 7; quedaba obsoleto).
- Nuevos ítems `[x]`: ML roundtrip en CI (+ bug del from_binary) y CI repair.

## docs/AGENTS.md

- Nueva **Nota ronda 8**: migración mlua, roundtrip ML, bug `from_binary`
  (~512 GB), CI reparada, tests 281 (232 unit).

## Coherencia

- El badge de CI del README vuelve a tener significado: los triggers de
  `ci.yml` estaban malformados y el workflow referenciaba `weaver`; tras la
  reparación, el badge refleja una pipeline que sí corre en `master`.
- Ningún doc queda con menciones a rlua como dependencia activa (solo
  histórico en informes/ROADMAP).
