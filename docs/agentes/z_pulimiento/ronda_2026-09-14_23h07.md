# Ronda 4 — Pulimiento (2026-09-14 23h07)

## Nueva documentación

- **`docs/EMULATION.md`** (nuevo): referencia autorizada de la política de
  emulación — principios (lab-only, enumeración real / ejecución simulada,
  telemetría etiquetada, sin código armable), matriz completa por módulo,
  política de comandos del TaskPoller y lista explícita de lo que NO volverá
  al repo.

## Documentación actualizada

- **README.md**: banner reposicionado como "Red-team swarm for labs you own"
  con enlace a la política; tabla de colonia honesta (drone sin "SSH
  propagation", honeybee "emulation-only", swarm "lógica simulada"); bucle de
  agentes ahora menciona el TaskPoller; "Honest limitations" actualizada
  (emulación global ronda 4, poller ya no es pendiente); índice de docs con
  EMULATION.md.
- **docs/DEPLOYMENT.md**: reescrito completo (760→~90 líneas) — build nativo y
  cross-compile, despliegue de laboratorio con `hive.sh colony/lab`, flujo del
  ejercicio con el TaskPoller, variables de entorno y verificación. Retiradas
  las secciones 2-7 de armado de payloads (deploy.sh, network stager, USB,
  phishing HTML+VBA, EXE C# loader, stager monolítico).
- **docs/OPERATOR_GUIDE.md**: Quick Start sin generación de payloads
  (build → colony → tui); árbol de scripts limpio (solo los 3 legítimos);
  flags de herramientas retiradas eliminadas; variables de entorno
  actualizadas (`HIVE_C2_API_KEY`, `HIVE_POLL_SECS`, `HIVE_LAB_AUTHORIZED`;
  fuera DNS/ICMP tunnel); troubleshooting con entradas del TaskPoller y del
  gate fileless; tabla de docs sin EVASION.md/MITRE_MAPPING.md (borrados en
  ronda 2) y con EMULATION.md.
- **docs/API.md**: sección "Task lifecycle (ronda 4)" en `POST /task` —
  formato real `shell_exec` + `{"cmd","session"}`, enrutado de resultados por
  beacon al WebSocket del operador y política de comandos.
- **docs/AGENTS.md**: nota de honeybee ampliada con la extensión de la
  política a todo el enjambre (ronda 4).
- **ROADMAP.md**: P1 actualizado — task poller ✅, C2 hardening ✅ (ronda 3),
  emulación ✅ (ronda 4); pendientes reales: ML converter, module theater,
  split con cargo features, mlua, tests loom.
- **scripts/lab_setup.sh**: banner sin el script de propagación eliminado,
  apunta a `docs/EMULATION.md`.

## Tests alineados con la política

- `saboteur.rs`: los 3 tests que asertaban mutación REAL de ficheros
  (source `<=`→`<`, `replicas: 3`→`2`, inyección sshd en logs) ahora asertan
  el contrato inverso: el fichero queda INTACTO y la respuesta es
  `simulated` — tests de seguridad, no solo de comportamiento.
- `privesc.rs`: tests de pwnkit/writable-cron (referencias a exploits
  eliminados) sustituidos por `test_attempt_escalation_is_simulated` (nunca
  root shell) y `test_scan_suid_binaries_readonly`.
- `tests/hive_colony_verify.sh`: Paso 7 "exfiltración real" → "ciclo de
  tareas (exfil simulada)"; Paso 8 loot informativo (ya no `fail` por
  diseño); Paso 9 propagación → movimiento lateral en emulación.
