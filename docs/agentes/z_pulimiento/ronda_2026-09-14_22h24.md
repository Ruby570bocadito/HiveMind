# Informe de Pulimiento — Ronda 2

- **Fecha:** 2026-09-14
- **Hora:** 22h24 (Europe/Madrid)
- **Agente:** z_pulimiento
- **Ronda:** 2

## Resumen

Pulido de la neutralización ampliada: todo el repositorio (docs, docker, helm, scripts, tests de laboratorio) quedó coherente con la eliminación del agente Weaver y de los módulos ofensivos. Cero referencias activas huérfanas.

## Detalle de cada cambio

1. **Docs eliminados:** `docs/EVASION.md` (técnicas de evasión defensiva-evasiva del framework) y `docs/MITRE_MAPPING.md` (mapeo técnica→módulo de código ya inexistente).
2. **Docs actualizados (9):** `README.md` (fila Weaver de la tabla de colonia, layout de `agents/`, crates 11→10, fila MITRE_MAPPING del índice de docs), `docs/README.md` (2 filas del índice), `docs/AGENTS.md` (diagrama principal, tabla resumen, diagrama de comunicaciones, capacidades de Drone, sección Weaver completa eliminada), `docs/NAMING.md` (fila Weaver/Morph), `docs/PLAYBOOK.md` (diagrama, comando, tabla de módulos, salida de validate, stack docker), `docs/DEVELOPMENT.md` (estructura: "Six binaries" → "Five binaries"), `docs/OPERATOR_GUIDE.md` (3 patrones ps/pkill), `docs/DEPLOYMENT.md` (7 diagramas/listas), `docs/API.md` (lista `X-Agent-Role`).
3. **Docker/helm/scripts/tests:** servicios y binarios weaver eliminados de Dockerfile, Dockerfile.lab, ambos compose, helm chart; listas de agentes en 6 scripts y 5 tests de laboratorio.
4. **Línea en blanco restaurada** en `docker-compose.lab.yml` entre servicios tras el corte del bloque.

## Archivos eliminados + motivo

| Archivo | Motivo |
|---------|--------|
| `agents/weaver/` (crate) | Motor de mutación polimórfica — evasión; aprobado por propietario |
| `hive_base/src/attack.rs` | Inventario ATT&CK + tagging de acciones ofensivas |
| `hive_base/src/lolbin.rs` | Ejecución vía LOLBins |
| `hive_base/src/larva.rs` | Generación de payloads hijos |
| `hive_base/src/wax.rs` | Sellado de payloads del dropper + mutación binaria |
| `hive_base/src/ephemeral.rs` | Código muerto (sin consumidores) dependiente de wax |
| `docs/EVASION.md` | Documentación de evasión |
| `docs/MITRE_MAPPING.md` | Mapeo de técnicas a módulos eliminados |

## Estado del README

Veraz y actualizado: la colonia tiene 5 agentes, 10 crates, y las limitaciones honestas incluyen la neutralización de ronda 1. La cifra de tests cambia esta ronda (−1: test de cobertura ATT&CK); verificar en CI antes de futuros ajustes.

## Estado de las dependencias

- `Cargo.toml`: workspace con 10 miembros; `Cargo.lock` regenerado por cargo sin el paquete weaver.
- `requirements.txt`: sin cambios esta ronda (pendiente decisión sobre `pandas`).
- `hive.toml`: conserva `weaver_mutation_interval_secs` por compatibilidad serde (campo sin efecto, marcado al Director).

## Estado del diseño web

Sin cambios: el README sigue siendo la cara pública y mantiene el diseño dark/neon profesional. Los diagramas ASCII de docs se regeneraron sin el box de Weaver y quedan alineados.

## Bugs marcados a z_bugs

1. `stinger/src/main.rs` ramas Windows: `launch("weaver", embedded::WEAVER)` referenciaba una constante eliminada → habría roto el build Windows (corregido).
2. `docker/lab/docker-compose.lab.yml`: YAML inválido preexistente (`networks: ive-net]`) dentro del bloque weaver → desapareció con el bloque (corregido de paso).
3. `stinger/build.rs`: bins de weaver en las listas de embed → advertencia engañosa de build (corregido).

## Tareas pendientes para la próxima ronda

- Decisión del propietario: `propaganda.rs` y resto de módulos ofensivos (ver riesgo #1/#2 del Director).
- Retirar campo legado `weaver_mutation_interval_secs` (rompe compat de hive.toml: decidir).
- Confirmación de tests en CI; convergencia master/main; `pandas` en requirements.
