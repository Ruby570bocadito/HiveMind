# Hive Colony — Python Dependencies (core: scout ML training)
# pip install -r requirements.txt
#
# Used by training/scout (dataset gen + classifier training + .bin export)
# and by the CI training-pipeline job. RL extras live in requirements-ml.txt.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# Optional ONNX interop export (NOT required by the runtime — the Rust worker
# consumes the .bin format from training/export_bin.py):
#   pip install skl2onnx
