# Hive Colony - Python Dependencies
# pip install -r requirements.txt

# Test infrastructure (C2 server + dashboard)
flask>=3.0
flask-cors>=4.0

# ML training (scout classifier + shaper RL)
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
joblib>=1.3
skl2onnx>=1.15
onnxruntime>=1.16

# RL training (optional, for shaper_policy.onnx)
gymnasium>=0.29
stable-baselines3>=2.0
torch>=2.0

# ONNX export/conversion
protobuf>=4.0
