# Hive Colony — ML Training Dependencies
# pip install -r requirements-ml.txt
# Required only for the shaper RL experiments (training/shaper/).
# The scout classifier needs just the core set in requirements.txt.

-r requirements.txt

# RL training (shaper_policy experiments: DQN/PPO)
gymnasium>=0.29
stable-baselines3>=2.0
