{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hive-colony.serviceAccountName" . }}
  labels: {{- include "hive-colony.labels" . | nindent 4 }}
{{- end }}
# Ronda 10: el ClusterRole privilegiado anterior (pods/nodes/secrets
# create/delete) no tiene consumidor — ningún binario de la colonia habla
# con la API de Kubernetes. Si tu lab añade un operador que necesite
# introspección, define ahí sus permisos concretos.
